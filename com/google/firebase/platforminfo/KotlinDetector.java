package com.google.firebase.platforminfo;

import androidx.annotation.Nullable;

public final class KotlinDetector {
    // 去混淆评级： 低(20)
    @Nullable
    public static String detectVersion() [...] // 潜在的解密器
}

